import React from 'react';
import { View, Text } from 'react-native';

const ProfileScreen = () => {
  return (
    <View>
      <Text>User Profile</Text>
      {/* Add profile details and edit functionality */}
    </View>
  );
};

export default ProfileScreen;
